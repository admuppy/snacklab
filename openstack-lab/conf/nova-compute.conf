# Overrides the driver snippet installed by nova-compute-ironic: the lab runs
# the fake driver — instances are pure state machines, no VMs, near-zero cost.
[DEFAULT]
compute_driver = fake.FakeDriver
