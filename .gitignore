# runtime state
v0.1/node_modules/
v0.1/sessions/
v0.1/.cookie-secret
v0.1/users.json
*.log
.DS_Store

# site-local config (never commit — may contain internal hosts/credentials)
chart/values-live.yaml
build.env

# agent scratch
.claude/
NOTES.txt
